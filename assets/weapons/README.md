# Bowbert Weapons

One subdirectory is one reusable weapon or weapon-like runtime asset.

- `bow/`: Bowbert's procedural branch bow. It contains source references, runtime tuning values, a browser tuning page, and exported preview states. The actual game texture is still generated at runtime by `src/render/player/proceduralBranchBow.ts`.
