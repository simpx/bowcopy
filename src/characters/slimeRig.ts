import rigJson from '../../assets/characters/slime/rig.json';

// Single source of truth: assets/characters/slime/rig.json (runtime section).
// Tune values in the workbench panel (saves back to rig.json) or edit the
// JSON directly; do not re-introduce literal values in this file.

export const SLIME_RIG = rigJson.runtime;
